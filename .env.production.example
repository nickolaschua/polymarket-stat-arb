# =============================================================================
# Polymarket Stat Arb — Production Environment Variables
# =============================================================================
#
# Copy this file to .env.production and fill in your values:
#   cp .env.production.example .env.production
#
# This file is sourced by:
#   - deploy/deploy.sh          (server bootstrap)
#   - deploy/start-collector.sh (systemd wrapper)
#   - deploy/docker-compose.prod.yml (Docker Compose)
#
# NEVER commit .env.production to git — it contains secrets.
# =============================================================================

# ---------------------------------------------------------------------------
# Database (Required)
# ---------------------------------------------------------------------------
# TimescaleDB credentials — used by Docker Compose and config.yaml
POSTGRES_USER=polymarket
POSTGRES_PASSWORD=   # CHANGE THIS — generate with: openssl rand -base64 32

# ---------------------------------------------------------------------------
# Wallet (Required for live trading, optional for data collection)
# ---------------------------------------------------------------------------
# Polymarket wallet private key.
# RECOMMENDED: Use age encryption instead of storing here.
#   1. Run: echo "0xYOUR_KEY" | bash deploy/encrypt-key.sh
#   2. Leave this variable empty — start-collector.sh decrypts automatically.
# FALLBACK: Set here if not using age encryption (less secure).
POLY_PRIVATE_KEY=

# ---------------------------------------------------------------------------
# Alerts (Optional)
# ---------------------------------------------------------------------------
# Telegram bot token for trade/opportunity alerts.
# Create a bot via @BotFather on Telegram to get a token.
TELEGRAM_BOT_TOKEN=
